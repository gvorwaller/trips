# 2026-06-18 Devlog

## 13:16 EST - P1/P2 batch: map buffer, template delete, insert-at-position, text documents

### Summary
Cleared four backlog tasks in one verified batch (one P1 + three P2) and shipped
to prod (`0696f29`). Each path was exercised live in Chrome against the test DB
before deploy.

### What Was Done
- **td-609ae7 (P1) — map zoom ignored 20km buffer.** Root cause: last session's
  buffer fix went into `MapPicker` (the editing picker), but the trip-detail map
  is a separate component, `PinMap`, which hard-coded `setZoom(13)` for a single
  pin. Replaced with `fitBounds(boundsAround(lat,lon,20))`. Verified: Blue Hill,
  ME frames regionally; Provence (2 pins) unchanged.
- **td-c7b9a1 — template delete.** Added `tmpl-delete` action + a per-template ✕
  chip routed through the existing shared confirm modal. FK `ON DELETE CASCADE`
  clears template items. Viewer-guarded. Verified delete → cascade → empty-state
  → re-seed.
- **td-4aa8c4 — insert packing item at a position.** Inline insert form opens
  exactly where the item will land (above/below any row), so a mid-list item
  arrives in place without repeated move-up/down. New `createPackingItemAt` +
  `placeNodeRelative` reuse `computeReparent` for a clean sequential reindex.
  Both branches verified; Enter submits. Drag-to-reorder filed as **td-4f7d9b
  (P3)** per the "buttons now, drag later" decision.
- **td-072807 — paste text as a document.** Migration `0003` adds `kind` +
  `text_content` to `attachments`, makes `object_key` nullable, and adds a shape
  CHECK tying each kind to its field. `createTextDocument` + `doc-text-add`
  action; text docs render inline (`<pre>` whitespace-preserved), delete skips
  the storage call (no object). Extended global search to match document titles
  and text bodies — verified a body-only term ("auberge") returns the doc.

### Files Modified
- `src/lib/components/PinMap.svelte` — single-pin 20km buffer
- `backend/db/migrations/0003_text_documents.sql` — new (kind/text_content)
- `src/lib/server/attachments.ts` — `createTextDocument`, kind in list,
  delete skips storage for text, download is file-only
- `src/lib/server/packing.ts` — `createPackingItemAt`
- `src/lib/server/tree-sql.ts` — `placeNodeRelative`
- `src/lib/server/search.ts` + `src/routes/search/+page.svelte` — document hits
- `src/routes/trips/[id]/+page.server.ts` — `tmpl-delete`, `pack-add-at`,
  `doc-text-add` actions
- `src/routes/trips/[id]/+page.svelte` — template ✕, inline insert form,
  text-doc paste + inline render

### Key Technical Insight
The "documents" UI is backed by the `attachments` table. Adding text docs there
(rather than a new table) reused the whole ownership/confirm-delete/listing path;
the only friction was relaxing file-shaped constraints — handled with a nullable
`object_key` + a shape CHECK rather than splitting the table. `td-c18799` (notes
on a packing item) will be even cheaper: `packing_items.notes` and the
`pack-edit`/`updatePackingItem` path already exist — it's UI-only.

### Next Steps
- **td-c18799** (packing-item notes) — small UI follow-up; backend already done.
- **td-4f7d9b** (drag-to-reorder, P3) — deferred polish.

## 13:40 EST - td-3a0e29: LLM reservation extraction (text + document)

### Summary
Auto-fill the Add Reservation form from a pasted confirmation email or an
already-uploaded document (PDF/image), using the existing Anthropic key. Shipped
to prod (`d5cfbf4`). Both paths verified live in the browser.

### What Was Done
- **`src/lib/server/reservation-extract.ts`** (new) mirrors giftlist's
  `llm-matcher`: raw `fetch` to the Anthropic Messages API (no SDK), key from
  `$env/dynamic/private`, structured output forced via a `submit_reservation`
  `tool_use` (never parsed from prose), 30s timeout, graceful `null` on
  missing-key / API error. `extractFromText` and `extractFromDocument` (text docs
  route through text; PDFs/images go through Claude vision as base64
  document/image blocks). Model default `claude-opus-4-7`, overridable via
  `ANTHROPIC_RESERVATION_MODEL`.
- `attachments.ts`: `getAttachmentSource` (ownership-checked). `storage.ts`:
  `getObjectBuffer` (read object into memory for vision input).
- `+page.server.ts`: `res-extract` action — returns candidate fields only, never
  creates a reservation.
- `+page.svelte`: auto-fill UI (paste-text + document-picker) binds results into
  a draft form for human review; added the previously-missing `status` field.

### Files Modified
- `src/lib/server/reservation-extract.ts` (new)
- `src/lib/server/attachments.ts`, `src/lib/server/storage.ts`
- `src/routes/trips/[id]/+page.server.ts`, `src/routes/trips/[id]/+page.svelte`
- prod-only: `ANTHROPIC_API_KEY` added to `/opt/trips/.env` (mode 600), reused
  from the giftlist/birds key; `pm2 restart trips --update-env`.

### Key Technical Insight
The extraction is strictly advisory: it pre-fills, the human reviews, then saves
via the existing `res-add`. It never auto-commits (extraction isn't 100%). The
`tool_use` `input_schema` instructs dates as `YYYY-MM-DDTHH:MM` (no tz), which
drops straight into the `datetime-local` inputs — no client date parsing needed.
Verified live: an Air France email and a generated Marriott PDF both filled
type/title/code/status/start/end/notes correctly (PDF via vision).

### Op note
While scanning sibling `.env` files for an existing key, a masking command
misfired and printed the key values into the session transcript. Keys are
unchanged/valid; flagged to the user to rotate `ANTHROPIC_API_KEY`,
`OPENAI_API_KEY`, `GEMINI_API_KEY` at their discretion.

### Next Steps
- **td-c18799** (packing-item notes) — small UI follow-up; backend already done.
- **td-4f7d9b** (drag-to-reorder, P3) — deferred polish.
- Optional: add a URL source for extraction (the deferred stretch from the task).

## 14:29 EST - Packing item notes (td-c18799) + drag-to-reorder (td-4f7d9b)

### Summary
Two packing-outliner improvements shipped together (`fc5efbf`): per-item notes
(and the first-ever packing-item edit UI), and drag-to-reorder. Verified against
the test DB.

### What Was Done
- **td-c18799 — notes.** Packing items had no edit affordance at all (the
  `pack-edit` action existed but was unused). Added a per-row `edit` details with
  name, quantity, and a Notes textarea posting to `pack-edit`; notes render inline
  under the item name. `category` is preserved through a hidden field because
  `pack-edit`/`updatePackingItem` set it directly (not COALESCE), so a notes-only
  save would otherwise clear it.
- **td-4f7d9b — drag-to-reorder.** New `pack-reparent` action over the existing
  `runReparent`/`computeReparent` (reindex + cycle/unknown-parent rejection).
  Per-row ⠿ HTML5 drag handle; dropping in a row's top/bottom half places the
  dragged item as a sibling before/after that target (`onRowDragOver` →
  `onRowDrop` computes parent+index, POSTs, `invalidateAll`). `dragListId` scopes
  a drag to its own list. Deeper re-nesting still uses indent/outdent; drag is
  desktop polish, buttons remain the primary touch path.

### Files Modified
- `src/routes/trips/[id]/+page.server.ts` — `pack-reparent` action (+`runReparent` import)
- `src/routes/trips/[id]/+page.svelte` — packing edit details, notes display,
  drag handle/handlers, styles

### Key Technical Insight
HTML5 native drag-and-drop does not fire from synthesized mouse events (CDP /
browser-automation limitation), so the OS-level gesture couldn't be driven
automatically. Verified instead by dispatching real `dragstart`/`dragover`/`drop`
DOM events — which run the actual Svelte handlers — and confirming the row moved
and persisted, plus direct `pack-reparent` action tests for cross-group reparent
(both groups reindex) and cycle rejection (`ok:false`, no change).

### Backlog
All P1/P2 cleared. Remaining: **td-b4ec6a** (Phase 5: print one-pager / export /
PWA) and **td-29a3ac** (link out to birds app), both P3.

## 17:29 EST - Print one-page trip sheet (td-a2d073); split Phase 5

### Summary
Shipped a printable trip sheet (`fc5efbf` → `26360ba`), the wanted slice of
Phase 5. Split td-b4ec6a so print could close on its own; the rest (export,
PWA-offline, headless PDF) stays deferred there.

### What Was Done
- Split **td-b4ec6a** ("Phase 5: export + PWA offline + deploy", whose
  description had been truncated by an earlier `td update`): created **td-a2d073**
  (print one-pager, P2) and rewrote td-b4ec6a as the deferred remainder
  (Markdown/HTML export · offline current-trip + queued sync · headless PDF).
  Deploy was already done long ago; offline deemed unnecessary (iOS
  Add-to-Home-Screen covers the install feel).
- **Print (td-a2d073):** a 🖨 Print button in the trip header (owner + viewer)
  expands all collapsed branches, `window.print()`, then restores the prior fold
  state (so nothing is missing from the printout and the on-screen view is
  undisturbed). A global `@media print` block in `app.css` strips nav/hamburger,
  map, all forms, row controls, drag handles, carets, collapse/expand controls,
  delete buttons, progress bars, map-link chips, and the confirm modal; lays out
  trip name + dates, places, packing (checkbox state + item notes), reservations,
  and text documents; `@page` margins; forces text-doc bodies visible. PDF comes
  free via the browser's Save-as-PDF — no headless PDF.

### Files Modified
- `src/app.css` — `@media print` block
- `src/routes/trips/[id]/+page.svelte` — Print button + `printSheet()` (expand/
  print/restore), `tick` import

### Key Technical Insight
Collapsed branches are removed from the DOM (not just hidden), so a CSS-only
print rule would omit them — hence `printSheet()` clears the collapse sets in
memory (without persisting), awaits `tick()`, prints, then restores. Verified in
Chrome's print preview (user-confirmed): chrome stripped, checkboxes show state,
reservation/notes laid out cleanly across pages.

### Also this session (earlier)
- iOS drag fix: hid the ⠿ handle on touch devices (HTML5 DnD doesn't fire on
  WebKit touch); real touch-drag filed as td-a8afca (P3).

### Backlog
All P1/P2 cleared. Remaining (all P3): td-b4ec6a (export / offline / headless
PDF — deferred), td-a8afca (touch drag), td-29a3ac (link out to birds).

## 19:?? EST - Print follow-up: tighter typography + spacing (f35748b)

### Summary
User feedback on the just-shipped print sheet: wanted a smaller font and
tighter spacing (the system print dialog gives no typography control), and
mused about rendering maps/docs as images. Tightened the print CSS; declined
the image work after weighing it. Shipped (`f35748b`), health green.

### What Was Done
- **Tighter print layout.** In the `@media print` block: shrank the print base
  from the 18px/1.5 screen scale to **9.5pt/1.25**, dropped headings
  (h1 20→16pt, h2 13→11pt, sub 11→9.5pt), reduced card/`@page` margins, and —
  the dominant fix — overrode the **44px touch tap-target `.line` height** to
  `min-height:0` + ~0.5pt padding (with `!important` to beat the component's
  scoped specificity), plus an 8pt `.meta` and an 11px print checkbox. Rows now
  sit on tight single lines; the test trip collapses from several airy pages
  toward a true one-pager.
- **Maps/docs as print images — assessed, declined.** Interactive Google maps
  don't print (blank tiles); the real fix would be a Static Maps `<img>`
  (needs the Maps Static API enabled in the gaylonphotos GCP project). Image
  attachments would be easy to embed; PDFs would need a client-side pdf.js
  rasterizer (most reservation docs are PDFs — highest value, most work). User
  chose to leave both as-is: the **reservation details already print**, which
  is the main benefit; the image idea was whimsical.

### Files Modified
- `src/app.css` — `@media print` typography/spacing tightening

### Key Technical Insight
The airy printout wasn't font size — it was the `.line { min-height: 44px }`
accessibility tap target bleeding into print, making every list row a half-inch
tall. Because that rule is component-scoped (Svelte hashes a class onto it,
raising specificity), a plain global print override loses; `!important` is
required to win. Verified by injecting the live stylesheet's `@media print`
rules as an on-screen `<style>` (faithful simulation minus `@page`) against the
test trip — rows compacted as expected.

### Backlog
Unchanged — all P3: td-b4ec6a, td-a8afca, td-29a3ac.

## 19:55 EST - CCC pre-flight backup script (6a65545)

### Summary
Added `scripts/backup-pg.sh`, a Carbon Copy Cloner pre-flight that snapshots
the prod Postgres DB + droplet-only config into `data/backup/`, with a
`--local-only` mode for the test DB. Cloned from `birds/scripts/backup-pg.sh`
and adapted to trips. Verified end-to-end against both DBs; user confirmed the
real CCC backup ran fine.

### What Was Done
- **Cloned + adapted** birds' backup-pg.sh. All droplet specifics were
  **verified live** (`pg_lsclusters`, nginx sites-available, `pm2 jlist`,
  `pg_database`) rather than copied: prod cluster `trips` on **5437**, config
  dir `/etc/postgresql/17/trips`, app `/opt/trips`, nginx
  `trips.gaylon.photos`, pm2 `trips`, prod DB `trips`.
- **Default run** SSHes the droplet, `pg_dump`s prod as the `postgres`
  superuser, and pulls `.env` (mode 600), nginx/postgresql/pg_hba confs, and a
  **secrets-redacted** pm2 jlist into `data/backup/prod/`, stamping
  `PULL_OK_AT`. Remote writes are confined to `/tmp` and cleaned up.
- **`--local-only`** dumps the test DB to `data/backup/local/`.
- **CCC-hardened**: ignores CCC's positional args, restores `PATH`, locates
  Homebrew `pg_*` bins, uses an explicit SSH key + known_hosts (works when CCC
  runs it as root), `chown`s the backup dir back to the owner on exit, and tees
  every run to `data/backup/preflight.log`.
- **gitignore**: added `data/` so the pulled prod `.env` and dumps never get
  committed (confirmed git ignores it).

### Files Modified
- `scripts/backup-pg.sh` — new
- `.gitignore` — ignore `data/`

### Key Technical Insight
The local dump must run as the **migration owner** (`trips_owner`), not the
runtime `trips_app` role: `trips_app` lacks `LOCK` privilege on
`admin.schema_migrations`, so an app-role `pg_dump` aborts with "permission
denied for schema admin". Prod sidesteps this by dumping as the `postgres`
superuser. Verified: `--local-only` and full prod pull each yield an
11-table dump that passes `pg_restore -l`; prod `.env` lands mode 600; pm2
dump has all PASSWORD/SECRET/TOKEN/API_KEY values redacted.

### Next Steps
- Attachment **blobs live in DO Spaces**, so `pg_dump` captures their metadata
  and object keys but not the file bytes. User is considering a separate pull
  to a Synology NAS to mirror the blobs (mirroring the madonnahist repo's image
  pull) — not yet set up.

## 20:10 EST - NAS Spaces blob backup scripts (8354cdd)

### Summary
Cloned the madonnahist Synology→Spaces blob pull for trips so the attachment
*bytes* (not just the `pg_dump` metadata/keys) get a local NAS copy. Repo
source-of-truth scripts + doc are committed; the NAS-side setup is a documented
runbook because it needs hands-on steps that I can't/shouldn't do.

### What Was Done
- **`scripts/nas/backup-spaces.sh`** — `rclone copy` (not sync, so prod deletes
  don't propagate) `do-trips:gaylon-trips` → `/volume3/gaylon-trips-spaces-backup/current`,
  with per-run + latest manifests, `LAST_RUN_STATUS`/`LAST_SUCCESS`/`LAST_FAILURE`,
  failure-only `synodsmnotify`, and `--dry-run`. Object count walks the whole
  `current/` tree (trips uses a single `trips/` key prefix).
- **`scripts/nas/snapshot-spaces-backup.sh`** — short-retention Btrfs snapshot
  (keep 2) of the share.
- **`docs/nas-spaces-backup.md`** — layout, semantics, and a one-time setup
  runbook.
- Both scripts pass `sh -n`. Env-var prefix is `TRIPS_` (vs madonnahist's
  `MADONNAHIST_`); cron offset 30 min so the two apps' pulls don't overlap.

### Key Technical Insight (verified live on the NAS)
- The NAS already has rclone v1.74.3 and a `do-spaces` remote (sfo3), but that
  Spaces key is **bucket-scoped to madonnahist** — `rclone size do-spaces:gaylon-trips`
  → `403 AccessDenied`. So trips needs its **own** `do-trips` remote built from
  the trips Spaces key (`/opt/trips/.env`); same sfo3 endpoint, different key.
- NAS `sudo` requires a password and `synosharesnapshot` is root-only, so the
  dedicated Btrfs share, cron entries, and snapshots must be created via DSM /
  with the user's hands — not scripted from here.

### Next Steps (NAS-side, user)
Per the runbook in `docs/nas-spaces-backup.md`: (1) create the
`gaylon-trips-spaces-backup` Btrfs share in DSM; (2) `rclone config create
do-trips …` with the trips Spaces key/secret (credential step — mine to guide,
yours to enter); (3) `scp` the two scripts to the share; (4) dry-run + first
live pull; (5) schedule via DSM Task Scheduler; (6) restore drill. Once the
`do-trips` remote exists I can drive steps 3–4 and the drill.
