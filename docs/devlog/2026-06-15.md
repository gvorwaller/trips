# 2026-06-15 — Phase 0: planning finalized + skeleton scaffolded

## Planning
- Reviewed `codex-trip-planner-V2-plan.md` against the real `birds` and `gaylonphotos` source and the live droplet allocations.
- Wrote `docs/trip-planner-V3-FINAL-plan.md` (authoritative). Marked V1/V2 superseded.
- Locked decisions: app-proxied **private** attachments (the shared gaylonphotos bucket is public-only); viewer may toggle packing `checked` (one write exception); roles `owner`/`viewer`; pulled in templates+duplicate, offline PWA, one-tap directions, global search.
- Confirmed free infra: dev `5179`, prod `3004`, Postgres `5437`/test `15437`, `/opt/trips`, PM2 `trips`.

## Phase 0 scaffold (this session)
- SvelteKit 2 + Svelte 5 + TS + adapter-node app. `db.ts`, `auth.ts`, `session.ts`, `hooks.server.ts` (auth gate + viewer packing-check exception + same-origin guard), `/api/health`, minimal layout/home, `app.css` tokens, PWA placeholders (manifest, favicon).
- `backend/db/migrate_pg.sh` + first migration `0001_schema.sql` (all 10 tables, CHECK constraints, tree/search indexes) + `0002_grants.sql`. `TRIPS_ENV=test` guards.
- Test-DB harness (`scripts/lib/test-env.sh` + `test-db-*.sh`) on cluster `15437`.
- `ecosystem.config.cjs` (port 3004, `BODY_SIZE_LIMIT=Infinity`), `scripts/deploy-to-DO.sh`, `deploy/nginx.conf` (`client_max_body_size 32m`).
- `cs.md`, `AGENTS.md`, `docs/mockups/{mockup.css,trips.html}`.

## Verified
- `npm run check` → 0 errors / 0 warnings. `npm run build` → ok.
- `npm run test:db:up` → both migrations applied; `\dt` shows all 10 tables, CHECK constraints present.
- Migration safety guards refuse prod DB name and reserved ports (5436 etc.).
- `trips_app` runtime role can SELECT. `/api/health` → `{"db":"ok","version":"dev"}`; unauthenticated `/` → 303 (auth gate, /login arrives Phase 1).

## Droplet-only items still open (before first deploy)
- `pm2 status` confirm 3004 free; create Postgres cluster on 5437 (trips_owner/trips_app, db `trips`); install nginx conf; Cloudflare DNS + TLS mode; add `trips.gaylon.photos/*` + `http://127.0.0.1:5179/*` to the shared Maps key; provision **private** Spaces bucket for attachments; make `/opt/trips` a git checkout.

## GitHub + DO Postgres provisioning (live)
- Repo: pushed to **github.com/gvorwaller/trips** (private), `main` tracking origin.
- Corrected stale infra assumptions by reading the droplet live: it's **2 vCPU / 3.8 GiB / 24 G** (hostname `…1vcpu-1gb…` is misleading — resized). Only **two** PG clusters actually run: madonnahist 5434, birds 5436. cs.md's "5433=BTC, 5435=prod tunnel" are NOT live on this droplet — nothing listens there. App ports 3000–3003 used; **3004 free**. **5437 free** (live-checked).
- Created **new isolated PG17 cluster `trips` on 5437** via `pg_createcluster 17 trips --port 5437 --start` (Debian-managed, matching siblings): own data dir `/var/lib/postgresql/17/trips`, `start.conf=auto` (boot-start), binds **127.0.0.1 + ::1 only**. Siblings untouched.
- Roles `trips_owner` (migrations) + `trips_app` (runtime); database `trips` owned by trips_owner. Passwords generated on the droplet (openssl rand -hex 24).
- Wrote `/opt/trips/.env` (mode 600 root:root) with PG creds + generated AUTH_SECRET + empty Maps/Spaces placeholders, PORT=3004.
- Applied migrations via a temporary SSH tunnel running our real `migrate_pg.sh`: **10 tables**, `admin.schema_migrations` = {0001,0002}, `trips_app` SELECT works. Tunnel closed after.

### Deferred by owner this session
- DO Spaces **private** bucket → Phase 4 (attachments). DNS `trips.gaylon.photos` → Phase 5 (deploy). No DO/Cloudflare browser changes made.

## Phase 1 — auth + trip shell (done)
- Login/logout (`/login` actions, argon2 verify, dummy-hash timing equalization, session cookie, sliding expiry). `scripts/create-user.mjs` upserts owner/viewer users.
- `src/lib/server/trips.ts`: owner-scoped `listTrips/getTrip/createTrip/updateTrip/deleteTrip` + `parseTripForm` validator (name required, date format + start≤end).
- Routes: `/` (trip list), `/trips/new`, `/trips/[id]` (detail + delete-with-modal), `/trips/[id]/edit`. Shared `$components/TripForm.svelte`.
- Responsive shell from mockup.css (now `src/app.css`): top-nav + bottom-nav, read-only banner + role tag for viewer, sign-out. Owner-only pages redirect viewers.

### Verified (curl against local test DB on 15437, owner + viewer seeded)
- Unauth GET / → 303 /login. Owner: login → list → create → detail → edit (rename) → delete; DB confirms 0 rows after delete.
- Viewer: sees trips + read-only banner; no New/Edit/Delete controls; GET /trips/new → 303 home; POST create → 403; POST delete → 403.
- Viewer packing-check exception (`PATCH /api/packing/check`) passes the guard (404 — route lands in Phase 2), proving the allow-list works without opening other writes.
- Same-origin guard: owner POST with `Origin: https://evil.example` → 403.
- `npm run check` 0/0, `npm run build` ok, `npm run lint` clean.

## Next: Phase 2 — places + packing outliners, templates + duplicate trip, global search.

## Phase 2 — outliners, templates, duplicate, search (done)
- **Pure tree logic** `$server/tree.ts` (no DB → unit-tested): `childrenOf`, `descendantIds`, `wouldCreateCycle`, `computeMove/Indent/Outdent/Reparent`, `orderParentsFirst`, `flattenTree`. 15 Vitest cases.
- `tree-sql.ts`: transactional apply (sequential reindex within sibling group). `itinerary.ts` + `packing.ts`: CRUD, multi-line paste, cross-parent rejection (`assertParentInTrip/List`).
- Trip detail page: nestable Places + Packing outliners, keyboard move/indent/outdent buttons, add + paste, inline edit (<details>), per-list progress bars.
- Viewer check-off via `PATCH /api/packing/check` (ownership enforced in one UPDATE join); all other viewer writes 403.
- `templates.ts`: seed Essentials, save-list-as-template, apply-into-trip (tree-preserving copy). `clone.ts`: `duplicateTrip` deep-copies itinerary + packing trees, resets checked.
- `search.ts` + `/search`: ILIKE union over trips/places/packing/reservations.

### Verified (curl + psql on test DB)
- Itinerary add/paste/indent/move → nesting persists across reload (Palais & Pont nested under Arles).
- Cross-parent: add with a foreign parent_id → rejected, 0 rows created.
- Packing: list+items+paste; owner & viewer check-off both set checked=t; viewer pack-add → 403.
- Templates: seed → 21 items; apply → new list with 21 items copied with nesting.
- Duplicate: 5/5 itinerary + nesting copied, 2 lists copied, copy has 0 checked while original keeps 2; viewer duplicate → 403.
- Search finds places/packing/trips. check 0/0, build ok, 15 tests, lint clean.

### Known minor: a tampered cross-trip parent_id currently returns 500 (thrown guard) rather than a graceful 400 — data integrity holds; cosmetic only. Real UI never sends cross-trip parents.

## Next: Phase 3 — Maps (links + one-tap Google/Apple directions, MapPicker, pins).
