# 2026-06-16 — Phase 3: maps

## Done & verified (headless)
- `$lib/maplinks.ts` (pure, 10 tests): Google/Apple/Directions links + multi-stop "directions for the day". Work from a name alone; precise with coords/place_id.
- Adapted `$lib/google-maps.ts` loader, `$server/geocode.ts` + auth-gated `/api/geocode`, `$components/MapPicker.svelte`.
- Itinerary places: Google/Apple/Directions chips; day/section rows get a "Directions for the day" route. Dedicated `/trips/[id]/place/[itemId]` location editor (MapPicker → setLocation/clearLocation). `PinMap` on trip detail (click pin → scroll/flash row). Graceful no-key fallback.
- Verified on test DB (dev:test, 5179): authed geocode returns a real result (Palais des Papes), coords persist, per-place + per-day dir links and location links render, PinMap mounts (no missing-key message). check 0/0, build ok, 25 tests, lint clean. Committed 5cef136.

## RESOLVED — Maps key origins added (with owner, via browser)
- Map *tiles* won't render until `http://127.0.0.1:5179/*` (dev) and `https://trips.gaylon.photos/*` (prod) are added to the Maps key's HTTP-referrer (Website) restrictions — GCP project gaylonphotos, key "Maps Platform API Key" (id b517f9c0-…). birds already has 127.0.0.1:5178/* + birds.gaylon.photos/*.
- Added `http://127.0.0.1:5179/*` + `trips.gaylon.photos/*` to the "Maps Platform API Key" Website restrictions (GCP project gaylonphotos), alongside the existing birds/gaylon.photos entries. The HTTP-referrer editor lives at the BOTTOM of the key page (below the 33-API list), not above it — that's why it looked absent earlier. Purely additive; existing entries untouched.
- VISUALLY CONFIRMED in-browser at 127.0.0.1:5179: trip detail renders the Google map with pins at saved coords (Palais des Papes + Pont d'Avignon), plus per-place Google/Apple/Directions chips and a per-day "Directions for the day" route. Phase 3 complete end-to-end.
- Local Maps keys copied into .env.test (gitignored) for dev rendering once origin is allowed.

---

# 2026-06-16 — Phase 4: reservations + app-proxied private attachments

## Built
- `$lib/filevalidate.ts` (pure, 13 tests): `MAX_ATTACHMENT_BYTES = 30 MB`, magic-byte `detectFileType` (PDF/JPEG/PNG/WebP/HEIC), `validateUpload`, `safeFilename`. Server is the source of truth; the browser's `accept=` is just a hint.
- `$server/storage.ts`: thin `@aws-sdk/client-s3` wrapper for the **private** Spaces bucket — `putObject` (ACL `private`), `getObject` (returns a Readable for streaming), `deleteObject`, `storageConfigured`. `forcePathStyle:false`, region/endpoint/bucket from `SPACES_*`.
- `$server/attachments.ts`: `uploadAttachment` (validate → putObject → INSERT; **rolls back the object if the INSERT throws**), `getAttachmentForDownload` (ownership via `JOIN trips`), `fetchObject`, `deleteAttachment` (delete object first, then row; on object-delete failure marks row `delete_pending` and returns false). Object key = `trips/{tripId}/{randomBytes(8).hex}/{safeName}`.
- `$server/reservations.ts`: CRUD + `parseReservationForm`; types accommodation/flight/restaurant/transport/other. `start_at`/`end_at` are `TIMESTAMPTZ`, formatted to `YYYY-MM-DDTHH:MM` at the edge.
- `GET /trips/[id]/attachments/[attId]`: authenticated download **proxy** — no public/CDN/signed URL ever. Streams from Spaces via `Readable.toWeb`, `Content-Disposition: inline`, `Cache-Control: private, no-store`. Ownership enforced (owner + viewer share `ownerId`); the hook permits GET for viewers.
- Trip detail page: Reservations section (badge/edit/delete/add) and Documents section (multipart upload, download links, delete; owner-only controls). Upload action guards `file instanceof File`, size ≤ 30 MB, then hands bytes to `uploadAttachment`.

## Verified LIVE against the real `gaylon-trips` Spaces bucket (dev:test, port 5179, trip 6)
- **Valid PDF upload** → 200, DB row `active`, object at `trips/6/…/valid.pdf`.
- **Download proxy** → byte-identical round-trip (69 B), headers `content-type: application/pdf`, `content-disposition: inline`, `cache-control: private, no-store`.
- **Disguised file** (`%`-less fake.pdf) → 400 "Unsupported file type"; no row, no object.
- **Oversized** (~31 MB) → 400 "File exceeds the 30 MB limit"; no row, no object.
- **Insert-failure rollback**: upload with bogus `reservation_id=999999` (FK violation) → insert throws, and the bucket `trips/` prefix stayed **empty** — the orphaned object was cleaned up.
- **Delete** (owner) → row gone, object gone (bucket empty), subsequent download 404.
- **Viewer (wife)**: download → 200 identical bytes; upload → 403; delete → 403. (Only the packing-check PATCH is a viewer write exception.)
- `npm run check` 0/0, `npm run build` ok, **38 tests**, `prettier --check` clean.

## Notes
- The 30 MB cap lives at four layers: client `accept`/size hint, route guard, nginx `client_max_body_size 32m`, and DB `CHECK size_bytes <= 31457280`.
- Spaces creds (`SPACES_*`, scoped key `trips-app`, Read/Write/Delete on `gaylon-trips` only) live in gitignored `.env.test` (local) and `/opt/trips/.env` (droplet, 0600) — never committed.

---

# 2026-06-16 — Phase 5 (partial): first production deploy to trips.gaylon.photos

## The "3 prerequisites" — corrected against LIVE droplet state (not docs/hostnames)
- **Droplet memory**: hostname says `…-1gb-…` but `free -h` shows **3.8 GiB total** (4009864 kB), 1.7 GiB available + 4 GiB swap. The hostname is stale from initial provisioning; it was resized. Do not cite memory from the hostname — only from `free -h`.
- **Maps keys on droplet**: already present in `/opt/trips/.env` (`PUBLIC_GOOGLE_MAPS_API_KEY`, `PUBLIC_GOOGLE_MAPS_MAP_ID`, `GOOGLE_GEOCODING_KEY`) — step already done.
- **Prod Postgres (cluster `trips` on 5437)**: already provisioned — roles `trips_owner`/`trips_app`, db `trips`, all 10 tables, both migrations applied. Separate Debian cluster (own data dir), distinct from birds(5436)/madonnahist(5434).

## Done
1. **Made the GitHub repo public** (was private). birds is public and the droplet pulls it over HTTPS with no stored creds; trips now matches, so `git pull --ff-only` works on the droplet. (User approved making it public.)
2. **Bootstrapped `/opt/trips` as a git checkout**: `git init -b main` + remote + `fetch --depth=1` + `reset --hard origin/main`. The existing `.env` (gitignored, mode 600) survived untouched.
3. **Ran `scripts/deploy-to-DO.sh --skip-push`**: npm ci → build → migrate (0 applied, both already present) → installed `deploy/nginx.conf` to sites-available + symlink, `nginx -t` ok, reload → `pm2 startOrReload` (trips = pm2 id 6, port 3004) → internal health `{"db":"ok","version":"d265ae2"}`.
4. **Cloudflare DNS** (browser, account 35f0…c28c, zone gaylon.photos): added **A record `trips` → 134.199.211.199, Proxied, TTL Auto** — identical to the existing birds/gaylon.photos/gifts records. Records went 8 → 9.

## Verified live
- `dig trips.gaylon.photos` → Cloudflare proxy IPs (104.21.12.60, 172.67.193.181), same as siblings.
- `https://trips.gaylon.photos/api/health` → **HTTP 200 `{"db":"ok","version":"d265ae2"}`** (Cloudflare TLS → nginx :80 → PM2 :3004 → PG :5437).
- `https://trips.gaylon.photos/` → 303 to `/login`, login page renders over valid TLS. nginx is HTTP-only on :80; Cloudflare terminates TLS at the edge (zone "Always Use HTTPS"), matching all siblings (no :443 listeners on origin).

## Remaining before sign-in works
- **Prod `users` table is EMPTY.** Owner + viewer accounts exist only in the local test DB. They must be created on prod with `scripts/create-user.mjs` (run on the droplet with `--env-file=/opt/trips/.env`) — left to the owner to run so passwords aren't handled by the agent.

## Still pending for Phase 5 proper
- Print one-pager + Markdown/HTML export, PWA offline current-trip cache + queued check-off sync, headless PDF (only after a memory proof — peak render spikes, not baseline). These are app features, not deploy infra.

---

# 2026-06-16 — Owner-only Settings screen + first owner bootstrap

## Why
User "absolutely detests" CLI for app tasks. Added an in-app admin UI so account
and password management never needs `create-user.mjs` again (after the one-time
owner bootstrap, which has no UI by design — no self-serve "create first admin").

## Built (commit d0605f5, deployed)
- `/settings` (owner-only): change own display name; change own password (current-
  password check, min 8, confirm); manage the single viewer — create (validated
  username `^[a-z0-9._-]{2,60}$`), rename, reset password.
- Security: a successful owner password change revokes the owner's OTHER sessions
  (keeps current, via `destroyUserSessions(id, currentToken)`); a viewer reset
  revokes ALL viewer sessions (force re-login). `/settings` is owner-only three ways:
  the existing hook redirects viewers + blocks their POSTs, and `load` + every action
  re-check `role === 'owner'`.
- `$server/users.ts` (getPasswordHash/getViewer/usernameTaken/updateDisplayName/
  updatePasswordHash/createViewer); `destroyUserSessions()` added to `$server/session.ts`.
- Settings link in top-nav and bottom-nav (owner only; bottom-nav matters since
  top-nav links are `display:none` < 640px).

## Verified
- Local test DB (5179): page load; name change; password wrong/mismatch/short all
  rejected; success + session revocation (2nd session 303, 1st 200, old pw rejected,
  new accepted); viewer create/login/rename/reset; 409 when viewer exists; viewer
  blocked (GET 303, POST 403). check 0/0, build ok, 38 tests, lint clean.
- **Prod over HTTPS**: provisioned owner `gaylon` (id 1, temp password, to be rotated
  by user in Settings on first login). Login 200, `/settings` 200 ("No viewer account
  exists yet"), home 200. Public version d0605f5, db ok.

## Bootstrap note
Owner `gaylon` created with a throwaway temp password via create-user.mjs (user chose
"temp password, I rotate it"). User's first action: Settings → Change password (this
also revokes the agent's test session), then create the viewer account in-app. No
further CLI needed for user management.

---

## 19:31 EST - CarbonFin OPML import + dev tooling (/nn, /review, claude-relay MCP)

### Summary
Imported the user's CarbonFin packing list (OPML) into the prod packing list with a
person-split match-merge, and brought the repo's local dev tooling in line with the
sibling repos (slash commands + claude-relay MCP).

### What Was Done
- **OPML packing import (prod data):** Parsed `Packing_list_-_Scandinavia_trip.opml`
  (CarbonFin export, three top sections: Madonna / Gaylon / Mutual) with Python
  ElementTree. Built a person-split tree: top-level **Gaylon** and **Madonna** groups,
  each carrying their own items plus the **Mutual** items *merged into matching
  categories* (Mutual "Electronics" folded into each person's `electronics`; loose
  shared items — walking sticks, first aid, umbrella — appended at the person's top
  level). Notes preserved (Amazon link, "waterproof the shoes", the big electronics note).
- Inserted via an idempotent match-merge script (`parent_id IS NOT DISTINCT FROM`,
  case-insensitive name match within parent, sequential `sort_order`) in a single
  transaction with a `--commit` flag (default dry-run). Dry-ran against prod first
  (read-only, rolled back): 71 inserts / 0 skips. Renamed the empty list (id 1) →
  **"Maine packing list"**, committed 71 rows. "Essentials" (21) untouched. Cleaned up
  the one-off script + JSON from `/opt/trips` afterward.
- **Slash commands:** Copied `/nn` and `/review` from birds into `.claude/commands/`.
  `nn.md` verbatim; `review.md` verbatim except the Codex "Repository context" paragraph
  rewritten for trips (owner/viewer model, outliner tree-move logic, viewer write-guard,
  attachment/Spaces safety, PG type-boundary gotchas → CLAUDE.md). gitignored per-machine
  Claude state (`.claude/settings.local.json`, `.claude/worktrees/`). Committed c784365, pushed.
- **claude-relay MCP:** Added at local scope via `claude mcp add` (matches birds exactly:
  `node ~/claude-relay/mcp-server.js`, `RELAY_URL=ws://localhost:9999`). `claude mcp list`
  → ✔ Connected. Stored in `~/.claude.json` under the trips project (not a repo file).

### Files Modified
- `.claude/commands/nn.md`, `.claude/commands/review.md` (new), `.gitignore`
- `~/.claude.json` (trips project mcpServers — local, not committed)
- prod DB only: `packing_lists` id 1 renamed, 71 `packing_items` rows inserted (no repo code)

### Key Technical Insight
- CarbonFin OPML uses single-quoted attrs, `_note` for notes, `&#10;` newlines — a real
  XML parser (Python ElementTree) handles entities cleanly; don't regex it.
- `claude-relay` is configured per-project at **local scope in `~/.claude.json`**, not via
  a committed `.mcp.json` — so "copy from birds" means `claude mcp add`, nothing to commit.
  Use the CLI, not hand-editing the 84 KB `~/.claude.json` (the live session also writes it).
- Process note (user feedback): on prod writes, stop at the dry-run and wait for explicit
  go — don't announce "I'll commit now" and commit in the same turn. Saved as a memory.

### Next Steps
- Restart the session so `/nn`, `/review`, and the claude-relay tools load.
- User to verify the imported list in-app; optionally rename the "From Notes" item or the
  Maine 2026 trip (OPML was a Scandinavia list).
- Phase 5 proper still pending: print one-pager + export, PWA offline cache + queued
  check-off sync, headless PDF (after a memory-spike proof).
