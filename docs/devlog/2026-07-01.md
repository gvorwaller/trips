# 2026-07-01

## Place Details + Ask AI About This Place (td-80cf30)

Implemented the full `td-80cf30` plan (`docs/2026-07-01-place-ai-interaction-plan-claude.md`, a revision of Codex's original plan doc after a review pass) across four phases, then fixed two rounds of independent review findings.

**Phase 0/1 — Place Details fact layer.** Verified Places API (New) actually works with the existing `GOOGLE_GEOCODING_KEY` before writing any code — it's a different product from the legacy Places calls already in `geocode.ts` (`X-Goog-*` headers, not a `?key=` query param; HTTP status codes, not a `status` JSON field). Added `src/lib/server/place-details.ts`: `getPlaceDetails(placeId, { forceRefresh })`, cached in the existing `api_cache` table under `place-details:{id}:v1` with an 18h TTL, a stale-data fallback on rate-limit/upstream-error (mirroring `weather.ts`'s existing pattern), and no live "open now" boolean — only the hours schedule, so a stale cache entry can't misreport whether a place is currently open.

**Phase 2 — Place workspace UI.** Extended `/trips/[id]/place/[itemId]` from a bare map editor into a workspace with a "Known Details" panel. Itinerary items have three distinct location states, not two: a Google `place_id`, or just coordinates (from an Apple Maps import or a manual pin, no Google id), or nothing at all. For the middle case, the page resolves a candidate Google place for the owner to confirm ("Link this match") rather than auto-linking. Reused the itinerary row's existing `📍 location` link as the entry point instead of adding a new control to an already dense row.

**Phase 3 — Ask AI About This Place.** Extracted `ai-notes.ts`'s Anthropic HTTP/error plumbing into `src/lib/server/anthropic-client.ts` (shared `callClaude`, `AnthropicError`, model constant) so the new `place-ai.ts` helper doesn't duplicate it. Owner-only question form, progressively enhanced (disable-while-pending, latest-answer-only), 500-char cap. The prompt is deliberately narrow: hedge everything, never invent hours/prices/tickets/schedules, say plainly when something needs official-source verification.

**Phase 4 — Refresh + AI-powered summaries.** Added an owner-only "Refresh details" action (bypass-cache + re-fetch-and-upsert, not a delete). Added Google's `generativeSummary` field to the Known Details panel — researched actual pricing before building it: it bills under the same "Place Details Enterprise + Atmosphere" SKU already touched by `rating`/hours, so it's $0 marginal cost at this app's scale. Verified the real response shape against a live call rather than trusting doc summarization, which turned out to matter — Google's own docs implied `disclaimerText` but the actual field is `disclosureText`. Built the required "Summarized with Gemini" attribution + report-content link, and deliberately did *not* feed the summary into the Ask AI prompt (display-only context, never fed to another AI as a "known" fact).

**Review round 1 (Codex, via claude-relay).** Two real findings: the resolve-candidate flow was using a plain reverse-geocode (nearest street address) instead of a title-biased Places search, so it could link the wrong business; and "Refresh details" could silently redirect as if it succeeded even when the upstream call failed, because `getPlaceDetails`'s stale-fallback (correct for a normal page load) also masked the explicit-refresh failure case. Fixed both — exported `placesTextSearch` from `geocode.ts` and switched to a title-biased, tightly-radius'd search; added an explicit `result.details.stale` check to the refresh action. Verified live: the old approach resolved "QA Downeast Scenic Railroad" to "66 US-1, Ellsworth, ME 04605, USA" (a street address); the new approach correctly finds the real "Downeast Scenic Railroad" business.

**Review round 2 (`/code-review` workflow, high effort).** Four more findings, two real bugs and two cleanup items. The `fetchedAt` field was a raw `pg` `Date` object typed and used as a `string` on both the fresh-cache-hit and stale-fallback paths — corrupted the "as of" timestamp embedded in the Ask AI prompt and was a latent `TypeError` risk. Also: fixing round 1's title-biased search introduced its own regression — it ran uncached on every page load, unlike every other Google Places call site in the app. Fixed by adding `placesTextSearchCached` to `geocode.ts` (mirrors the existing `placesNearbyCached`, 60min TTL). Deduplicated a status-message map and a `MAX_QUESTION_LENGTH` constant that had drifted into separate server/client copies, into two new shared modules (`src/lib/place-details-status.ts`, `src/lib/place-ai-shared.ts` — `$server` modules can't be imported into a Svelte component, so these had to be plain universal modules).

**Also this session:** committed a permanent Safari QA helper (`scripts/safari-smoke-place-workspace.mjs`, run via `npm run test:safari:place-workspace`) using `safaridriver` + `selenium-webdriver`, replacing an earlier throwaway script. It self-provisions its own fixtures (dedicated login user, throwaway trip/items), is guarded behind `TRIPS_ENV=test`, and cleans up after itself — verified idempotent across repeated runs. Documented the general `safaridriver` pattern (and its gotchas — a login-form hydration race, `until.urlIs()` being useless for same-URL redirects, `process.exit()` truncating output on a real crash) in the global `~/.claude/CLAUDE.md` for reuse across other repos.

Verification:
- `npm run check` — 0 errors/warnings throughout
- `npm test` — 85 passing (started at 0 for this feature; grew across each phase)
- `git diff --check`
- `npm run build`
- Manual QA in both Chrome and Safari against the local test DB: login, all three location states, resolve-candidate → link → Known Details, Ask AI (grounded + limited modes), Refresh details (success and simulated-failure paths), AI summary rendering with attribution
- Live curl verification of every server action's success/failure paths, including deliberately breaking `GOOGLE_GEOCODING_KEY` to force the refresh-failure path
